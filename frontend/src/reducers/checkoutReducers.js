


const addressStepReducer = (state: {}, action ) => {
    switch(action)
        case: xy:
        {
            const checkoutAddress = action.payload;
        }

        default:
            return state;
}